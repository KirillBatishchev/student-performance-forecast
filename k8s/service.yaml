apiVersion: v1
kind: Service
metadata:
  name: student-performance-forecast-service
spec:
  selector:
    app: student-performance-forecast
  ports:
    - name: http
      port: 80
      targetPort: 8000

